spring:
  datasource:
    url: jdbc:mysql://192.168.0.106:3306/study_db?serverTimezone=UTC
    username: root
    password: pwd
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
  logging.level:
    org.hibernate.SQL: debug
